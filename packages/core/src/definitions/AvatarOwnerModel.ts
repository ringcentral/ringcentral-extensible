interface AvatarOwnerModel {
  /**
   * A token to access avatar image from CDN. Available only for authenticated panelists
   * Example: wedyugwied8wud98u90i329silqdjalidj
   */
  avatarToken?: string;
}

export default AvatarOwnerModel;
