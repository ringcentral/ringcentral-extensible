/**
 * Meeting host
 */
interface Host {
  /**
   * Account id
   */
  accountId?: string;

  /**
   * Extension id
   */
  extensionId?: string;

  /**
   * Display name
   */
  displayName?: string;
}

export default Host;
