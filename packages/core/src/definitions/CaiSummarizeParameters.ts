/**
 * Query parameters for operation caiSummarize
*/
interface CaiSummarizeParameters {
  /**
   * The webhook uri to which responses will be posted
   * Format: uri
   */
  webhook?: string;
}

export default CaiSummarizeParameters;
