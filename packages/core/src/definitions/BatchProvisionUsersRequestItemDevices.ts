import DeviceDefinition from "./DeviceDefinition.js";

interface BatchProvisionUsersRequestItemDevices {
  /** */
  deviceInfo?: DeviceDefinition;
}

export default BatchProvisionUsersRequestItemDevices;
