/**
 * Query parameters for operation patchNoteNew
*/
interface PatchNoteNewParameters {
  /**
   * If true then note lock (if any) will be released upon request
   */
  releaseLock?: boolean;
}

export default PatchNoteNewParameters;
