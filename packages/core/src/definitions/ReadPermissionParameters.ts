/**
 * Query parameters for operation readPermission
*/
interface ReadPermissionParameters {
    /**
   * Internal identifier of a service plan
   */
  servicePlanId?: string;
}

export default ReadPermissionParameters;
