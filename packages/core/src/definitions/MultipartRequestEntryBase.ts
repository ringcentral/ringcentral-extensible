interface MultipartRequestEntryBase {
  /**
   * Internal identifier of a resource (required when alternative syntax with '*' is used)
   * Example: 1724099032020
   */
  resourceId?: string;
}

export default MultipartRequestEntryBase;
