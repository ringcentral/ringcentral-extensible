interface PhoneNumberSenderConsentRecord {
    /**
   * Phone number in e.164 format (with '+' prefix)
   * Required
   * Example: +16501234567
   */
  from?: string;
}

export default PhoneNumberSenderConsentRecord;
