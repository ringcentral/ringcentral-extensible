// Error response
class ErrorResponse {
  /**
   * Error code
   */
  errorCode?: string;

  /**
   * Human-readable description of an error. Not suitable for end users
   */
  description?: string;
}

export default ErrorResponse;
