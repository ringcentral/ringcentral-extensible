class ConferencingSettingsInfo {
  /**
   * Delegated access code for a host user
   */
  hostCode?: string;

  /**
   * Delegated access code for a participant user
   */
  participantCode?: string;
}

export default ConferencingSettingsInfo;
