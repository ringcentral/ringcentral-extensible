import { DateTime } from './index';

class GateScheduleOverride {
  overrideClosedEvent?: string;

  overrideDate?: DateTime;

  overrideLabel?: string;

  overrideSched?: string;

  scheduleOverrideId?: number;
}

export default GateScheduleOverride;
